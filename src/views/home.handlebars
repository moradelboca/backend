{{#if noProducts}}
  <div>
    <p>No hay productos!</p>
  </div>
{{else}}
  <ul>
    {{#each paginateData.docs}}
      <li>
        <p>{{this.title}}: ${{this.price}}</p>
      </li>
    {{/each}} 
  </ul>
{{/if}}
